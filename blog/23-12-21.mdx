While the original zely server is processing one request, it cannot process another request.

By enabling the `experimental.useWorker` function, which uses the very cool `worker` feature in nodejs,
you can process other requests while processing a request.
